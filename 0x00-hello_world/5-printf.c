#include <stdio.h>
/**
 * main- the program's entry point
 *
 * Return : 0 if there is no error and non zero otherwise
 */
int main(void)
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
